{
  "name": "stylan-resume",
  "version": "0.8.0",
  "private": true,
  "description": "在线简历编辑器 - Markdown 编辑、模板定制、PDF 导出",
  "scripts": {
    "dev:web": "pnpm --filter @stylan/web dev",
    "dev:api": "cd apps/api && poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev": "concurrently \"pnpm:dev:web\" \"pnpm:dev:api\"",
    "build:web": "pnpm --filter @stylan/web build",
    "test:web": "pnpm --filter @stylan/web test",
    "test:api": "cd apps/api && poetry run pytest",
    "lint:web": "pnpm --filter @stylan/web lint",
    "install:all": "pnpm install && cd apps/api && poetry install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
